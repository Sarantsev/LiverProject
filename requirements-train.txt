# Точные пины рабочего окружения (Python 3.10) для удалённой GPU-машины.
# torch ставится ОТДЕЛЬНО (см. scripts/setup_env.sh), т.к. нужен CUDA-индекс.
#
# Установка:
#   pip install -r requirements-train.txt
# (предварительно поставить torch — см. setup_env.sh)

# --- SegVol / foundation ---
monai==0.9.0
einops==0.6.1
transformers==4.30.0
huggingface-hub==0.16.4
tokenizers==0.12.1

# --- научный стек ---
numpy==1.26.4
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.7.2
matplotlib==3.10.9

# --- медицинские форматы / IO ---
nibabel==5.4.2
SimpleITK==2.5.5
pyyaml

# --- радиомика ---
# pyradiomics НЕ ставим здесь: его сборка через pip падает из-за изоляции build-env
# (ModuleNotFoundError: numpy) + битые метаданные 3.1.0. Ставится отдельным шагом
# в scripts/setup_env.sh:
#   pip install "cython<3" numpy==1.26.4
#   pip install pyradiomics==3.0.1 --no-build-isolation
# Фолбэк: conda install -c conda-forge pyradiomics
# Радиомика опциональна — без неё пайплайн работает (graceful skip).

# --- утилиты ---
tqdm==4.68.2
tensorboardX==2.6.5
requests==2.28.1
