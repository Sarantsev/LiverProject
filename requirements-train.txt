# Точные пины рабочего окружения (Python 3.10) для удалённой GPU-машины.
# torch ставится ОТДЕЛЬНО (см. scripts/setup_env.sh), т.к. нужен CUDA-индекс.
#
# Установка:
#   pip install -r requirements-train.txt
# (предварительно поставить torch — см. setup_env.sh)

# --- SegVol / foundation ---
monai==0.9.0
einops==0.6.1
transformers==4.30.0
huggingface-hub==0.16.4
tokenizers==0.12.1

# --- научный стек ---
numpy==1.26.4
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.7.2
matplotlib==3.10.9

# --- медицинские форматы / IO ---
nibabel==5.4.2
SimpleITK==2.5.5
pyyaml

# --- радиомика ---
pyradiomics            # если не собирается через pip — см. conda-фолбэк в setup_env.sh

# --- утилиты ---
tqdm==4.68.2
tensorboardX==2.6.5
requests==2.28.1
